AWSTemplateFormatVersion: '2010-09-09'

Description: The template used to create an ECS Cluster from the ECS Console.

Parameters:

  ECSClusterName:
    Type: String
    Description: Specifies the ECS Cluster Name with which the resources would be associated
    Default: everlasting-gecko-ub6ome

  SecurityGroupIds:
    Type: CommaDelimitedList
    Description: Optional - Specifies the Comma separated list of the Security Group Id of an existing Security Group.
    Default: ''

  VpcId:
    Type: String
    Description: Optional - Specifies the ID of an existing VPC in which to launch your container instances. If you specify a VPC ID, you must specify a list of existing subnets in that VPC. If you do not specify a VPC ID, a new VPC is created with at least 1 subnet.
    Default: ''
    AllowedPattern: '^(?:vpc-[0-9a-f]{8,17}|)$'
    ConstraintDescription: VPC Id must begin with 'vpc-' and have a valid uuid

  SubnetIds:
    Type: CommaDelimitedList
    Description: Optional - Specifies the Comma separated list of existing VPC Subnet Ids where ECS instances will run
    Default: ''

Resources:

# https://docs.aws.amazon.com/AWSCloudFormation/latest/TemplateReference/aws-resource-ecs-cluster.html
  CruddurECSCluster:
    Type: AWS::ECS::Cluster
    Properties:

      ClusterName: !Ref CruddurECSCluster

      CapacityProviders:
        - FARGATE
#        - FARGATE_SPOT

      ClusterSettings:
        - Name: containerInsights
          Value: disabled

      Configuration:
        ExecuteCommandConfiguration:
          Logging: DEFAULT

      Tags:
        - Key: Environment
          Value: Production

#https://docs.aws.amazon.com/AWSCloudFormation/latest/TemplateReference/aws-resource-ecs-service.html
  CruddurFrontend:
    Type: AWS::ECS::Service
    Properties:
      AvailabilityZoneRebalancing: enabled
#      CapacityProviderStrategy: 
#        - CapacityProviderStrategyItem
      Cluster: !Ref CruddurECSCluster
#      DeploymentConfiguration: 
#        DeploymentConfiguration
#      DeploymentController: 
#        DeploymentController
      DesiredCount: 1
      EnableECSManagedTags: true
      EnableExecuteCommand: true
#      ForceNewDeployment: 
#        ForceNewDeployment
      HealthCheckGracePeriodSeconds: 60
      LaunchType: FARGATE
      LoadBalancers: 
        # - AdvancedConfiguration: 
        # - AdvancedConfiguration
        - ContainerName: cruddur-frontend
        - ContainerPort: 3000
#        - LoadBalancerName: String
        - TargetGroupArn: String
#      Monitoring: 
#        MonitoringConfiguration
      NetworkConfiguration: 
        AwsvpcConfiguration:
          AssignPublicIp: true
          SecurityGroups: 
            - String
          Subnets: 
            - String


#      PlacementConstraints: 
#        - PlacementConstraint
#      PlacementStrategies: 
#        - PlacementStrategy
#      PlatformVersion: String
      PropagateTags: SERVICE
#      Role: String
#      SchedulingStrategy: String
      ServiceConnectConfiguration: 
 #       AccessLogConfiguration: 
 #         ServiceConnectAccessLogConfiguration
        Enabled: true
        # LogConfiguration: 
        #   LogConfiguration
        Namespace: cruddur
        Services: 
          ClientAliases: 
#            DnsName: String
            Port: 3000
#            TestTrafficRules: 
#              ServiceConnectTestTrafficRules
          DiscoveryName: cruddur-frontend
#          IngressPortOverride: Integer
          PortName: cruddur-frontend
#          Timeout: 
#            TimeoutConfiguration
#          Tls: 
#            ServiceConnectTlsConfiguration    
      ServiceName: cruddur-frontend
      # ServiceRegistries: 
      #   - ServiceRegistry
      Tags: 
        - Key: Environment
          Value: Production
      TaskDefinition: cruddur-frontend
#      VolumeConfigurations: 
#        - ServiceVolumeConfiguration
#      VpcLatticeConfigurations: 
#        - VpcLatticeConfiguration


  CruddurBackend:
    Type: AWS::ECS::Service
    Properties:
      AvailabilityZoneRebalancing: enabled
#      CapacityProviderStrategy: 
#        - CapacityProviderStrategyItem
      Cluster: !Ref CruddurECSCluster
#      DeploymentConfiguration: 
#        DeploymentConfiguration
#      DeploymentController: 
#        DeploymentController
      DesiredCount: 1
      EnableECSManagedTags: true
      EnableExecuteCommand: true
#      ForceNewDeployment: 
#        ForceNewDeployment
      HealthCheckGracePeriodSeconds: 60
      LaunchType: FARGATE
      LoadBalancers: 
        # - AdvancedConfiguration: 
        # - AdvancedConfiguration
        - ContainerName: cruddur-backend
        - ContainerPort: 4567
#        - LoadBalancerName: String
        - TargetGroupArn: String
#      Monitoring: 
#        MonitoringConfiguration
      NetworkConfiguration: 
        AwsvpcConfiguration:
          AssignPublicIp: true
          SecurityGroups: 
            - String
          Subnets: 
            - String
#      PlacementConstraints: 
#        - PlacementConstraint
#      PlacementStrategies: 
#        - PlacementStrategy
#      PlatformVersion: String
      PropagateTags: SERVICE
#      Role: String
#      SchedulingStrategy: String
      ServiceConnectConfiguration: 
 #       AccessLogConfiguration: 
 #         ServiceConnectAccessLogConfiguration
        Enabled: true
        # LogConfiguration: 
        #   LogConfiguration
        Namespace: cruddur
        Services: 
          ClientAliases: 
#            DnsName: String
            Port: 3000
#            TestTrafficRules: 
#              ServiceConnectTestTrafficRules
          DiscoveryName: cruddur-backend
#          IngressPortOverride: Integer
          PortName: cruddur-backend
#          Timeout: 
#            TimeoutConfiguration
#          Tls: 
#            ServiceConnectTlsConfiguration    
      ServiceName: cruddur-backend
      # ServiceRegistries: 
      #   - ServiceRegistry
      Tags: 
        - Key: Environment
          Value: Production
      TaskDefinition: cruddur-backend
#      VolumeConfigurations: 
#        - ServiceVolumeConfiguration
#      VpcLatticeConfigurations: 
#        - VpcLatticeConfiguration

Outputs:

  ECSCluster:
    Description: The created cluster.
    Value: !Ref ECSCluster

