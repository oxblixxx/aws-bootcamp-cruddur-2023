version: "3.8"
services:
  backend-flask:
    image: prod-backend:v1
#    image: 193654356005.dkr.ecr.us-east-1.amazonaws.com/backend-flask:latest
    ports:
      - "4567:4567"
    volumes:
      - ./backend-flask:/backend-flask
    environment:
      AWS_ENDPOINT_URL: "http://dynamodb-local:8000"
      CONNECTION_URL: "${CONNECTION_URL}" 
      #FRONTEND_URL: "https://${CODESPACE_NAME}-3000.app.github.dev"
      FRONTEND_URL: "https://front.mustaphaops.online"
      #FRONTEND_URL: "https://ubiquitous-carnival-r57qvrvv65jfw74v-3000.app.github.dev"
      #BACKEND_URL: "https://${CODESPACE_NAME}-4567.app.github.dev"
      BACKEND_URL: "https:/back.mustaphaops.online"
      #BACKEND_URL: "https://ubiquitous-carnival-r57qvrvv65jfw74v-4567.app.github.dev"
      # HONEYCOMB
      # SERVICE NAME AS TO BE UNIQUE AS PER DIFFERENT SERVICES.
      OTEL_SERVICE_NAME: 'backend-flask'
      OTEL_EXPORTER_OTLP_ENDPOINT: "https://api.honeycomb.io"
      OTEL_EXPORTER_OTLP_HEADERS: "x-honeycomb-team=${HONEYCOMB_API_KEY}"
      # XRAY
      #AWS_XRAY_URL: "*4567-${GITPOD_WORKSPACE_ID}.${GITPOD_WORKSPACE_CLUSTER_HOST}*"
      AWS_XRAY_DAEMON_ADDRESS: "xray-daemon:2000"                 
      AWS_DEFAULT_REGION: "${AWS_DEFAULT_REGION}"
      AWS_ACCESS_KEY_ID: "${AWS_ACCESS_KEY_ID}"
      AWS_SECRET_ACCESS_KEY: "${AWS_SECRET_ACCESS_KEY}"
      FLASK_ENV: development
      FLASK_DEBUG: "1"
      #ROLLBAR_ACCESS_TOKEN: "${ROLLBAR_ACCESS_TOKEN}"      
      AWS_COGNITO_USER_POOL_ID: "us-east-1_4RnA4Ed2q"
      AWS_COGNITO_USER_POOL_CLIENT_ID: "6nscku3k5ij847p4drsfis74ge"    
     #build: ./backend-flask
    #healthcheck:
    # test: ["CMD-SHELL", "curl --fail http://localhost:4567/health || exit 1"]
    #  interval: 30s
    #  timeout: 10s
    #  retries: 3    aa
  frontend-react-js:
    image: cruddur-front-staging:v1
   #build: ./frontend-react-js    
    ports:
      - "3000:3000"
    environment:
      #REACT_APP_BACKEND_URL: "https://${CODESPACE_NAME}-4567.app.github.dev"
      REACT_APP_BACKEND_URL: "https://back.mustaphaops.online"
      #REACT_APP_BACKEND_URL: "https://ubiquitous-carnival-r57qvrvv65jfw74v-4567.app.github.dev"
      REACT_APP_AWS_PROJECT_REGION: "${AWS_DEFAULT_REGION}"
      REACT_APP_AWS_COGNITO_REGION: "${AWS_DEFAULT_REGION}"
      REACT_APP_AWS_USER_POOLS_ID: "${REACT_APP_AWS_USER_POOLS_ID}" 
      REACT_APP_CLIENT_ID: "${REACT_APP_CLIENT_ID}"     
      #REACT_APP_WEB_CLIENT_SECRET: "${REACT_APP_WEB_CLIENT_SECRET}" 
    #image: cruddur-frontend:v1
    
    volumes:
      - ./frontend-react-js:/frontend-react-js
    #depends_on:
    #healthcheck:
    #  test: ["CMD-SHELL", "curl --fail http://localhost:3000/health || exit 1"]
    #  interval: 30s
    #  timeout: 10s
    #  retries: 3
  xray-daemon:
    image: "amazon/aws-xray-daemon"
    environment:
      AWS_ACCESS_KEY_ID: "${AWS_ACCESS_KEY_ID}"
      AWS_SECRET_ACCESS_KEY: "${AWS_SECRET_ACCESS_KEY}"
      AWS_REGION: "us-east-1"
    command:
      - "xray -o -b xray-daemon:2000"
    ports:
      - 2000:2000/udp      
  dynamodb-local:
    #https://stackoverflow.com/questions/67533058/persist-local-dynamodb-data-in-volumes-lack-permission-unable-to-open-databa
    #We needed to add user:root to get this working.
    user: root
    command: "-jar DynamoDBLocal.jar -sharedDb -dbPath ./data"
    image: "amazon/dynamodb-local:latest"
    container_name: dynamodb-local
    ports:
      - "8000:8000"
    volumes:
      - "./docker/dynamodb:/home/dynamodblocal/data"
    working_dir: /home/dynamodblocal     


  pg-db-local:
    image: postgres:13-alpine
    restart: always
    environment:
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=password
    ports:
      - '5432:5432'
    volumes: 
      - db:/var/lib/postgresql/data  


#opentelemetry container
# otel-collector:
#   image: "otel/opentelemetry-collector"
#   environment:
#     HONEYCOMB_API_KEY: "${HONEYCOMB_API_KEY}" # Same honeycomb api key used in the backend enviroment
#   command: [--config=/etc/otel-collector-config.yaml]
#   volumes:
#     - ./otel-collector-config.yaml:/etc/otel-collector-config.yaml
#   ports:
#     - 14268:14268
#     - 4318:4318
# the name flag is a hack to change the default prepend folder
# name when outputting the image names
networks: 
  internal-network:
    driver: bridge
    name: cruddur


volumes:
  db:
    driver: local    
